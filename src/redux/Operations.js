// import { getData } from "../services/api";
// import { setContacts } from "./contactsActions";

// const API_ENDPOINT = "contacts";

// const getContacts = () => (dispatch) => {
//   getData(API_ENDPOINT).then((contacts) => dispatch(setContacts(contacts)));
// };

// export { getContacts };
